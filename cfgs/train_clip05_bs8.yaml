# Training config with clip_norm=0.5, batch_size=8 (for dual training on 1 GPU)
lr: 3e-4
epochs: 100
batch_size: 8
num_workers: 4
pin_memory: true

# AMP settings
amp_dtype: bf16
amp_enabled: true

# Gradient clipping (L2 norm)
clip_norm: 0.5

# Output directory
checkpoint_dir: runs


