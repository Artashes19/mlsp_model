# Training config for minimal train.py
lr: 3e-4
epochs: 100
batch_size: 8
num_workers: 4
pin_memory: true

# AMP settings
amp_dtype: bf16    # or fp16
amp_enabled: true

# Gradient clipping (L2 norm); set 0 to disable
clip_norm: 1.0

# Output directory
checkpoint_dir: runs
